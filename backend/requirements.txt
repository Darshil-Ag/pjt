fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0
pydantic-settings>=2.2.0
python-dotenv>=1.0.0
pyyaml>=6.0.1

# Orchestration
langgraph>=0.1.0
langchain-core>=0.2.0

# LLM providers (free-tier)
groq>=0.8.0
google-genai>=1.0.0

# Vector store (local, free)
chromadb>=0.5.0

# Math
numpy>=1.26.0
scikit-learn>=1.4.0    # For Cohen's kappa + ablation metrics

# Dataset / utilities
tabulate>=0.9.0        # kappa_check pretty output

# Persistence (HITL checkpoint — F-09)
supabase>=2.0.0        # Supabase free tier — HITL state persistence across restarts

# Testing
pytest>=8.2.0
pytest-asyncio>=0.23.0
httpx>=0.27.0          # FastAPI test client
